"""Voice calling support for the `poly call` command.

Bootstraps and drives a WebRTC voice call to an agent, mirroring the browser
in-Studio calling flow. Currently scoped to draft/branch calls.
"""
